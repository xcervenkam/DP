# models

This folder contains the saved models of the project.

## Structure

- [`deployment/`](deployment/README.md)  
  Final models selected for reuse together with their metadata.

## Role in the Project

Unlike `data/processed/model_runs`, which stores detailed run outputs, this folder concentrates the models intended for later scoring and practical reuse.
